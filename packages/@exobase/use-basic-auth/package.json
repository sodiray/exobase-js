{
  "name": "@exobase/use-basic-auth",
  "version": "1.0.0-rc.0",
  "description": "Exobase basic authentication hook",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "jest --coverage",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "dependencies": {
    "radash": "^7.1.0",
    "@exobase/core": "^2.0.0-rc.1"
  }
}
