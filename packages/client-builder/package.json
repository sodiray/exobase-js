{
  "name": "@exobase/client-builder",
  "version": "1.0.0-alpha.1",
  "description": "Exobase api client builder",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "test": "jest --coverage",
    "check": "yarn lint && yarn test && yarn build",
    "build": "tsc",
    "lint": "tslint -p tsconfig.json",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "devDependencies": {
    "@types/chai": "^4.2.21",
    "@types/jest": "^27.0.1",
    "chai": "^4.3.4",
    "jest": "^27.1.0",
    "ts-jest": "^27.0.5",
    "tslint": "^6.0.0",
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "radash": "^2.2.1"
  }
}
