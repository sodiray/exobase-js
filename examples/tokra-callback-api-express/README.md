# Exobase Example: Callback API on Express.js

> An example project using Exobase to run functions on Express.js

## Running the Project

First run `yarn` to install dependencies, then run `yarn dev` to start the API locally.
