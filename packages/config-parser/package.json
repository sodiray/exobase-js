{
  "name": "@exobase/config-parser",
  "version": "0.0.0",
  "description": "Exobase utility for parsing Exobase yml configuration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "test": "ts-node ./src/tests/parser.test.ts",
    "check": "yarn lint && yarn test && yarn build",
    "build": "tsc",
    "lint": "tslint -p tsconfig.json",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "devDependencies": {
    "@exobase/client-js": "^0.0.16",
    "@types/baretest": "^2.0.0",
    "baretest": "^2.0.0"
  },
  "dependencies": {
    "@exobase/core": "^0.0.5",
    "js-yaml": "^4.1.0",
    "radash": "^1.3.0"
  }
}
