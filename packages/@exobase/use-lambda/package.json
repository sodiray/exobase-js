{
  "name": "@exobase/use-lambda",
  "version": "1.0.0-rc.1",
  "description": "Exobase root hook to handle function running on AWS Lambda",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "jest --coverage",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.97"
  },
  "dependencies": {
    "@exobase/core": "^2.0.0-rc.1"
  }
}
